import { PrivatePlaceholder } from "@/components/layout/private-placeholder";

export default function LowStockPage() {
  return (
    <PrivatePlaceholder
      description="Produtos abaixo do mínimo e compras sugeridas serão implementados na fase de reposição."
      eyebrow="Reposição"
      title="Reposição / Compras"
    />
  );
}
